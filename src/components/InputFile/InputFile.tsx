import styled from 'styled-components';
import Button from '../../components/Button/Button';

export const InputFile = styled.input`
  display: none;
`;

export const InputFileLabel = styled(Button)`

`;